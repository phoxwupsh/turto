services:
  bot:
    build: .
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
    volumes:
      - ./config.exmaple.toml:/app/config.toml
      - ./help.exmaple.toml:/app/help.toml
      - ./templates.exmaple.toml:/app/templates.toml